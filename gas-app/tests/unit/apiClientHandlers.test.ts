/**
 * apiClientHandlers.test.ts — DEPRECATED
 *
 * The API_CLIENT role and api_key authentication method were removed in Phase 1.
 * All functionality is now accessed via upload links. These tests are kept as
 * stubs to avoid deletion noise in git history but all suites are skipped.
 */

describe.skip('apiClientHandlers (deprecated — API_CLIENT role removed)', () => {
  it('placeholder', () => { /* removed */ });
});
