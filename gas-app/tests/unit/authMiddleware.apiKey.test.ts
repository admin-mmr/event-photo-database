/**
 * authMiddleware.apiKey.test.ts — DEPRECATED
 *
 * The API_CLIENT role and authenticateApiKey() were removed in Phase 1.
 * All volunteer access is now via upload links. These tests are kept as
 * stubs to avoid deletion noise in git history but all suites are skipped.
 */

describe.skip('authenticateApiKey (deprecated — API_CLIENT role removed)', () => {
  it('placeholder', () => { /* removed */ });
});
